import { Theme } from "@emotion/react";

const themes: { [key: string]: Theme } = {
  light: { colors: { primary: "#3A5A40", background: "#E7EDBD" } },
  dark: { colors: { primary: "#E7EDBD", background: "#3A5A40" } },
};
export default themes;
